{
  "name": "zenozeno-torch",
  "version": "0.0.1",
  "description": "Flask server for a GPT-2-based quote generator",
  "main": "server.py",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
